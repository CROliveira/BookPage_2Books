# BookPage_2Books

## COMPLETE FILE SUMMARY

All 7 files are now complete. Here is what Alex uploads to Namecheap cPanel → File Manager → `public_html/`:

```
public_html/
├── index.html          ✅ Homepage
├── story.html          ✅ Our Story — Celio backgrounder
├── capabilities.html   ✅ Core Capabilities — frameworks + speaking
├── books.html          ✅ Books Hub — both books, author strip, offers, claim form
├── book-ci.html        ✅ Constitutional Intelligence — full CI experience
├── book-edt.html       ✅ Étoile de Tournesol — full EDT experience
├── contact.html        ✅ Contact — form, FAQ, direct emails
└── images/
    ├── celio_photo.jpg     ← Add headshot here
    ├── CI_cover.jpg        ← Add CI cover here
    └── EDT_cover.jpg       ← Add EDT cover here
```

**Before going live — Alex replaces all `REPLACE_WITH_` placeholders:**

| Placeholder | Where to get it |
|---|---|
| `REPLACE_WITH_CI_STRIPE_LINK` | dashboard.stripe.com → Payment Links → Create |
| `REPLACE_WITH_CI_PAYHIP` | payhip.com → Products → CI → Share link |
| `REPLACE_WITH_CI_PAYONEER` | myaccount.payoneer.com → Request Payment |
| `REPLACE_WITH_CI_AMAZON` | Confirm with FriesenPress via books.friesenpress.com |
| `REPLACE_WITH_EDT_STRIPE_LINK` | Same — separate link for EDT |
| `REPLACE_WITH_EDT_PAYHIP` | Same — separate EDT product |
| `REPLACE_WITH_EDT_PAYONEER` | Same — separate EDT request |
| `REPLACE_WITH_*_FORMSPREE_ID` | formspree.io → New Form → copy ID |
| `REPLACE_WITH_CONTACT_FORMSPREE_ID` | Same — third form for contact page |
